# *** zsh always executes this file first ***

# (OPTIONAL) Don't load global zsh configuration from /etc.
# setopt no_global_rcs

# Source zsh configuration from directory below. Files are executed in this order:
# .zprofile -> .zshrc -> .zlogin -> .zlogout
export ZDOTDIR=~/.zsh

